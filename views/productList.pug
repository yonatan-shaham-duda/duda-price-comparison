html 
    head 
        title=group.name
        style
            include styles.css
    body
        div.flex-container
            div.flex-sixty.middle
                img.singleImage.center(src=group.products[0].images[0].url)
            div.flex-thirty.middle
                div
                    h1=group.name
                    div !{group.products[0].description}
                    div.bestPrice Best price: $555
                    div 
                        button.buyButton BUY NOW
        
        //- h3=group.name
        //- div.flex-container.card
        //-     div.info.flex-container 
        //-         div !{group.products[0].description}
        //-         div
        //-             img.mainImage(src=group.products[0].images[0].url)
        //-     div.list
        //-         each product in group.products
        //-             include product.pug
        //- script
        //-     | var group = !{JSON.stringify(group)};
        //-     | console.log(group)